import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-app-capacitor-plugins',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
